# 原始專案計畫

## 專案目標

建立一個 Python 腳本，用於解決一個簡單的線性迴歸問題。

## 核心要求

1.  **遵循 CRISP-DM 流程:** 整個腳本的執行過程和輸出需要明確對應到 CRISP-DM 的六個階段（業務理解、數據理解、數據準備、模型建立、模型評估、部署）。
2.  **互動性:** 允許使用者在腳本執行時，透過命令列 `input` 來設定以下參數：
    - `a`: 真實數據的斜率。
    - `noise`: 數據的噪聲程度。
    - `number of points`: 生成數據點的數量。
3.  **詳細輸出:** 腳本不能只有程式碼和最終結果，必須包含每個步驟的說明文字 (prompt and 過程)。
4.  **單一文件:** 所有功能都在一個 Python 腳本 (`.py` file) 中完成。
