# 待辦事項 (Todo)

以下是一些可以為這個專案增加更多功能的想法：

### 功能增強

- [ ] **允許使用者上傳自己的數據集:** 新增一個檔案上傳器，讓使用者可以上傳自己的 CSV 檔案來進行分析。
- [ ] **新增更多模型:** 加入多項式迴歸 (Polynomial Regression) 或其他迴歸模型，讓使用者可以進行比較。
- [ ] **新增更多評估指標:** 除了 MSE 和 R²，可以加入 MAE (平均絕對誤差) 等其他指標。
- [ ] **下載模型/圖表:** 新增按鈕讓使用者可以下載訓練好的模型 (使用 `pickle` 或 `joblib`) 或生成的圖表。
- [ ] **主題切換:** 允許使用者在淺色和深色主題之間切換。

### 程式碼與結構

- [ ] **重構程式碼:** 將 CRISP-DM 的每個步驟拆分成獨立的函式，讓 `app.py` 更簡潔。
- [ ] **增加單元測試:** 為數據處理和模型訓練的關鍵函式編寫測試案例。
