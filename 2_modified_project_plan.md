# 修改後專案計畫

## 專案目標

將原有的命令列腳本重構成一個互動式的 Streamlit Web 應用程式，以提供更現代化、更友好的圖形化使用者介面 (GUI)。

## 核心要求

1.  **Web 化:** 使用 Streamlit 框架將整個應用程式變成一個可以透過瀏覽器存取的 Web App。
2.  **GUI 互動:** 
    - 使用 Streamlit 的 `st.sidebar` 和 `st.slider` 等元件來取代原有的 `input()`，讓使用者可以透過滑桿即時調整 `a`（斜率）、`noise`（噪聲）和數據點數量。
    - 每次調整後，應用程式應自動重新計算並更新結果，提供即時反饋。
3.  **結構化呈現:** 
    - 使用 `st.expander` 來組織 CRISP-DM 的六個步驟，讓使用者可以清晰地查看每個階段的細節。
    - 使用 `st.metric` 來美化和突顯關鍵的評估指標（如 MSE, R²）。
    - 使用 `st.pyplot` 來嵌入 Matplotlib 生成的圖表。
4.  **專案結構化:** 
    - 將專案拆分成多個文件，建立一個清晰的目錄結構。
    - `app.py`: 核心應用程式碼。
    - `requirements.txt`: 管理專案依賴。
    - `README.md`: 提供專案說明和執行指南。
    - `.md` 文件: 包含開發日誌、專案計畫、待辦事項等，使專案管理更標準化。
